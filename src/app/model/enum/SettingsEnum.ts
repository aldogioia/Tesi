export enum Font {
  noto = 'noto',
  helvetica = 'helvetica',
  comic = 'comic',
  atkinson = 'atkinson'
}

export enum Mode {
  light = '',
  dark = 'dark',
  auto = 'auto'
}

export enum Layout {
  compact = '',
  extended = 'extended',
  mixedSX = 'mixed-sx',
  mixedDX = 'mixed-dx'
}
